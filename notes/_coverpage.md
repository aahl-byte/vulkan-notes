<!-- _coverpage.md — the landing splash. Tailor the title + tagline per topic. -->

# vulkan gpu notes

> So Vulkan is... a lot

...it hands you every detail the driver used to hide — powerful, but a lot to hold in
your head at once. This project tries to break it down into pieces that actually fit:
the plain-language idea behind each one first, the API call second.

<pre class="cover-tree"><span class="root">foundation</span>     <span class="lbl">the shared core</span>
<span class="b">├──</span> compute    <span class="lbl">a shader over a buffer</span>
<span class="b">├──</span> rendering  <span class="lbl">pixels on screen</span>
<span class="b">└──</span> advanced   <span class="lbl">sync, memory, real frames</span></pre>

[start reading](/home.md)
[search](/search.md)
